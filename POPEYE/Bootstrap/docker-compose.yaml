version: '3'

services:
  jigglypuff-server:
    container_name: conteneur1
    build: .
    ports:
      - "8080:3000"
    environment:
      - JIGGLYPUFF=shiny
  
  nginx:
    container_name: conteneur2
    image: nginx:1.24-alpine
    ports:
      - "5000:80"
